import { FastifyRequest } from "fastify";
import { CreateDriverService } from "../services/CreateDriverService";

class CreateDriverController{
    async handle(request: FastifyRequest, reply: FastifyRequest) {
        
    }
}